 'use client';

import Image from 'next/image';

import { useEffect, useRef } from 'react';

export default function Projects() {

  return (
    <section
      className="relative w-full min-h-[100vh] bg-[#0A0A0B] overflow-hidden py-10 lg:py-16"
    >
     
    </section>
  );
}
